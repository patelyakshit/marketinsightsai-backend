# Application settings
APP_NAME=MarketInsightsAI
DEBUG=false

# CORS - Comma-separated list of allowed frontend origins
# Add your Vercel frontend URL here in production
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://your-app.vercel.app

# Database (use Render/Railway/Supabase PostgreSQL in production)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/database

# JWT Authentication (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-key-change-in-production

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Google Gemini (for image generation)
# Get API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key
# Latest model with image generation support
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview

# Google OAuth - Get from Google Cloud Console > APIs & Services > Credentials
# Create OAuth 2.0 Client ID for Web Application
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Esri/ArcGIS API Keys
# Data/Statistics Key - for GeoEnrichment, Tapestry data, demographics API calls
ARCGIS_DATA_API_KEY=your-arcgis-data-api-key

# Location Services Key - if backend needs geocoding/routing
ARCGIS_LOCATION_API_KEY=your-arcgis-location-api-key

# Report settings
REPORTS_OUTPUT_PATH=./reports

# Supabase Storage (for persistent cloud storage)
# Get these from Supabase Dashboard > Project Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_STORAGE_BUCKET=reports

# Backend URL (for generating full URLs when Supabase Storage not configured)
# Set this to your Railway/Render backend URL in production
BACKEND_URL=https://your-backend.railway.app
